import { LucideIcon } from "lucide-react";

export interface AboutStatsI {
    title: string;
    value: string;
    Icon: LucideIcon;  // React icon component
    bgColor: string;   // Tailwind background color class
}